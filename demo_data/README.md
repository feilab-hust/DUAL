# Demo Data

Place the input TIFF file in this directory and name it:

    test.tif

Supported input dimensions:

    T × Y × X (default)
    T × Z × Y × X

The default input path is:

    demo_data/test.tif

To use another file name or location, modify `datasets.val.dataroot` in `config/demo_config.json`.
